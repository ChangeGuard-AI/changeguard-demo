# The namespace budget: 1 CPU and 1Gi of memory for all pods together.
# 3 replicas use 300m CPU. 4 fit. 20 would need 2 CPU.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: demo-quota
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
